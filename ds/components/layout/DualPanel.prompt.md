Use DualPanel when two blocks are peers and the split itself carries meaning (data vs people, BLYN vs AI).

```jsx
<DualPanel divider="gradient" left={<DefinitionList items={registry} />} right={<MemberGrid />} />
```

- `divider="gradient"` only when the two sides map to the gold and violet axes.
