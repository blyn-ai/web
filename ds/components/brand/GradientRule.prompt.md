Use GradientRule directly under the site header, or across the top of a social card.

```jsx
<SiteHeader />
<GradientRule />
```

- One rule per surface. It is the only place the full four-stop gradient appears as a solid band.
- `height={6}` on 1200x630 cards; `height={3}` everywhere else.
